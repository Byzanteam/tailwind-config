const flatVariables = require('./flat-variables')
const getCustomProperties = require('./get-custom-properties')
const transformConfig = require('./transform-config')

module.exports = {
  flatVariables,
  getCustomProperties,
  transformConfig,
}
